name: Build Worker

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "**"
  pull_request:
    branches:
      - main

jobs:
  build:
    uses: ybojko/voting-app-ci/.github/workflows/build.yml@master
    permissions:
      contents: read
      packages: write
    with:
      image_name: worker
      context: .
      platforms: linux/amd64,linux/arm64
    secrets:
      GIT_OPS_TOKEN: ${{ secrets.GIT_OPS_TOKEN }}
      GHCR_TOKEN: ${{ secrets.GIT_OPS_TOKEN }}
